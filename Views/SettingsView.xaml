<UserControl x:Class="LaunchMate.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:launchMate="clr-namespace:LaunchMate"
             xmlns:viewModels="clr-namespace:LaunchMate.ViewModels"
             xmlns:local="clr-namespace:LaunchMate.Views" d:DataContext="{d:DesignInstance Type=viewModels:SettingsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.Resources>
        <Style x:Key="HelpTextStyle" TargetType="Run">
            <Setter Property="FontSize" Value="20"/>
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <TabControl x:Name="tabControl" TabStripPlacement="Left" Margin="0, 25,0,0">
        </TabControl>
        <Button Content="Save" Width="75" Height="30" Command="{Binding SaveCmd}" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>